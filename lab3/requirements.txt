# Lab 1 runnable stack — pin to the versions your account/toolkit expects.
bedrock-agentcore>=0.1.0            # BrowserClient + BedrockAgentCoreApp runtime harness
bedrock-agentcore-starter-toolkit>=0.1.0   # `agentcore` CLI (configure / launch / invoke)
playwright>=1.45                   # drives the AgentCore Browser session over CDP
boto3>=1.34
# google-adk>=2.0                  # optional: only if you deploy agent.py (ADK graph) as entrypoint
