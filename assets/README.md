# assets/

Put your automation image here.

The root `README.md` references:

    ./assets/automation-overview.png

Drop a file at that path (or update the `<img src=...>` in the root README to match your
filename) and the image will render at the top of the repo overview.
